#!/bin/bash

input="$1"
shift

sample="$1"
shift

for file in "$@"; do
        filename=$(basename -- "$file")
        extension="${filename##*.}"
        filename="${filename%.*}"
        g++ -o $filename $file
        ./$filename < $input > $filename.txt
        kompare $filename.txt $sample
done
